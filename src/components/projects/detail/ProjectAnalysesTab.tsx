"use client";

import Link from "next/link";
import { useFrameworkAnalyses } from "@/lib/frameworks/FrameworkAnalysesProvider";
import { analysesForProject } from "@/lib/frameworks/relations";
import { readFrameworkResult } from "@/lib/frameworks/schemas";
import { FRAMEWORK_LABEL } from "@/lib/frameworks/types";
import type { Project } from "@/lib/projects/types";
import { AnalysisVisualMap } from "@/components/frameworks/AnalysisVisualMap";

export function ProjectAnalysesTab({ project }: { project: Project }) {
  const { status, analyses, links } = useFrameworkAnalyses();
  const related = analysesForProject(analyses, links, project.id);

  if (status === "loading") return <Message>分析を読み込んでいます。</Message>;
  if (status !== "ready") return <Message>分析を表示するにはログインとSupabase設定が必要です。</Message>;
  return <div className="scroll-area min-h-0 flex-1 overflow-y-auto bg-white"><div className="mx-auto w-full max-w-[820px] px-5 py-6 sm:px-8"><div className="flex items-center justify-between gap-3"><h2 className="text-[16px] font-semibold text-[var(--color-ink)]">分析 <span className="tabular ml-1 text-[13px] font-normal text-[var(--color-ink-muted)]">{related.length}件</span></h2><Link href="/frameworks" className="text-[13px] text-[var(--color-zenith)] underline underline-offset-2">分析を追加</Link></div>{related.length === 0 ? <p className="mt-6 text-[14px] leading-7 text-[var(--color-ink-muted)]">紐づいた分析はありません。フレームワーク画面で分析を作成し、このプロジェクトを選択してください。</p> : <div className="mt-5 space-y-8">{related.map((analysis) => { const result = readFrameworkResult(analysis); return <article key={analysis.id} className="border-b border-[var(--color-line)] pb-7"><p className="label-caps">{FRAMEWORK_LABEL[analysis.framework_type]}</p><h3 className="mt-1 text-[19px] font-semibold text-[var(--color-ink)]">{result?.title ?? analysis.company_name}</h3><p className="mt-1 text-[12px] text-[var(--color-ink-muted)]">取得日時：{analysis.source_fetched_at ? new Date(analysis.source_fetched_at).toLocaleString("ja-JP") : "記録なし"}</p>{result ? <><p className="mt-3 text-[14px] leading-7 text-[var(--color-ink-secondary)]">{result.executiveSummary}</p><AnalysisVisualMap result={result} compact /><div className="mt-5 space-y-4">{result.sections.map((section) => <section key={section.id}><h4 className="text-[14px] font-semibold text-[var(--color-ink)]">{section.title}</h4>{section.keyInsight && <p className="mt-1 border-l-2 border-[var(--color-zenith)] pl-2 text-[13px] font-medium leading-6 text-[var(--color-ink)]">結論：{section.keyInsight}</p>}<p className="mt-1 whitespace-pre-wrap text-[13px] leading-6 text-[var(--color-ink-secondary)]">{section.analysis}</p>{section.evidence.length > 0 && <ul className="mt-1 list-disc pl-5 text-[12px] leading-6 text-[var(--color-ink-muted)]">{section.evidence.map((evidence) => <li key={evidence}>{evidence}</li>)}</ul>}{section.implications?.length ? <p className="mt-1 text-[12px] leading-6 text-[var(--color-ink-secondary)]">示唆：{section.implications.join(" / ")}</p> : null}</section>)}</div>{result.priorityActions?.length ? <section className="mt-5"><h4 className="label-caps">優先アクション</h4><ol className="mt-2 space-y-2 text-[13px] leading-6 text-[var(--color-ink-secondary)]">{result.priorityActions.map((item, index) => <li key={item.action}><span className="font-medium">{index + 1}. {item.action}</span><br /><span className="text-[12px] text-[var(--color-ink-muted)]">成功指標：{item.successSignal}</span></li>)}</ol></section> : result.recommendations.length > 0 ? <section className="mt-5"><h4 className="label-caps">推奨アクション</h4><ul className="mt-2 list-disc pl-5 text-[13px] leading-6 text-[var(--color-ink-secondary)]">{result.recommendations.map((item) => <li key={item}>{item}</li>)}</ul></section> : null}{result.limitations.length > 0 && <p className="mt-4 text-[12px] leading-6 text-[var(--color-ink-muted)]">注意：{result.limitations.join(" / ")}</p>}</> : <p className="mt-3 text-[13px] text-[var(--color-ink-muted)]">この分析結果は読み取れませんでした。</p>}<a href={analysis.source_url} target="_blank" rel="noreferrer" className="mt-4 inline-block text-[12px] text-[var(--color-zenith)] underline underline-offset-2">根拠URLを開く</a></article>; })}</div>}</div></div>;
}

function Message({ children }: { children: React.ReactNode }) { return <div className="px-6 py-10 text-[14px] text-[var(--color-ink-muted)]">{children}</div>; }
